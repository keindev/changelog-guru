export enum ServiceProvider {
    GitHub = 'github',
    GitLab = 'gitlab',
}

export enum Change {
    Major = 'major',
    Minor = 'minor',
    Patch = 'patch',
}

export enum Exclusion {
    Type = 'type',
    Scope = 'scope',
    Subject = 'subject',
    Author = 'author',
}
